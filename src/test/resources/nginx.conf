# Black List
routes.blacklist = ["localhost"]

routes.apis = [
    {
      name = "Test API1",
      listenPath = "file:/home/docker/log/nginx",
      targetUrl = "http://localhost:9000/service1/api1"
    }
  ]